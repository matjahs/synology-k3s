apiVersion: v1
kind: ConfigMap
metadata:
  name: guacamole-initdb
  namespace: tools
data:
  initdb.sql: |
    --
    -- Licensed to the Apache Software Foundation (ASF) under one
    -- or more contributor license agreements.  See the NOTICE file
    -- distributed with this work for additional information
    -- regarding copyright ownership.  The ASF licenses this file
    -- to you under the Apache License, Version 2.0 (the
    -- "License"); you may not use this file except in compliance
    -- with the License.  You may obtain a copy of the License at
    --
    --   http://www.apache.org/licenses/LICENSE-2.0
    --
    -- Unless required by applicable law or agreed to in writing,
    -- software distributed under the License is distributed on an
    -- "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
    -- KIND, either express or implied.  See the License for the
    -- specific language governing permissions and limitations
    -- under the License.
    --

    --
    -- Table of connection groups. Each connection group has a name.
    --

    CREATE TABLE guacamole_connection_group (

      connection_group_id   serial       NOT NULL,
      parent_id             integer,
      connection_group_name varchar(128) NOT NULL,
      type                  varchar(32)  NOT NULL DEFAULT 'ORGANIZATIONAL',

      -- Concurrency limits
      max_connections          integer,
      max_connections_per_user integer,
      enable_session_affinity  boolean NOT NULL DEFAULT FALSE,

      PRIMARY KEY (connection_group_id),

      CONSTRAINT connection_group_name_parent UNIQUE (connection_group_name, parent_id),

      CONSTRAINT guacamole_connection_group_ibfk_1
          FOREIGN KEY (parent_id)
          REFERENCES guacamole_connection_group (connection_group_id) MATCH SIMPLE
          ON UPDATE NO ACTION
          ON DELETE SET NULL

    );

    --
    -- Table of connections. Each connection has a name, protocol, and
    -- associated set of parameters.
    --

    CREATE TABLE guacamole_connection (

      connection_id   serial       NOT NULL,
      connection_name varchar(128) NOT NULL,
      parent_id       integer,
      protocol        varchar(32)  NOT NULL,

      -- Concurrency limits
      max_connections          integer,
      max_connections_per_user integer,

      -- Load-balancing behavior
      connection_weight integer,
      failover_only     boolean NOT NULL DEFAULT FALSE,

      -- Associated sharing profile for recording
      proxy_hostname    varchar(512),
      proxy_port        integer,
      proxy_encryption_method varchar(4),

      PRIMARY KEY (connection_id),

      CONSTRAINT connection_name_parent UNIQUE (connection_name, parent_id),

      CONSTRAINT guacamole_connection_ibfk_1
          FOREIGN KEY (parent_id)
          REFERENCES guacamole_connection_group (connection_group_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE SET NULL

    );

    --
    -- Table of users. Each user has a unique username and a hashed password
    -- with corresponding salt. Although the salt is not strictly necessary when
    -- using modern password hashing algorithms, it is provided for future
    -- compatibility.
    --

    CREATE TABLE guacamole_user (

      user_id       serial       NOT NULL,

      -- Username and optionally-hashed password
      entity_id     integer      NOT NULL,
      password_hash bytea        NOT NULL,
      password_salt bytea,
      password_date timestamptz,

      -- Account disabled/expired status
      disabled  boolean NOT NULL DEFAULT FALSE,
      expired   boolean NOT NULL DEFAULT FALSE,

      -- Time-based access restriction
      access_window_start    time,
      access_window_end      time,

      -- Date-based access restriction
      valid_from  date,
      valid_until date,

      -- Timezone used for access window/date comparisons
      timezone varchar(64),

      -- Profile information
      full_name           varchar(256),
      email_address       varchar(256),
      organization        varchar(256),
      organizational_role varchar(256),

      PRIMARY KEY (user_id)

    );

    --
    -- Table of user groups. Each user group has a name.
    --

    CREATE TABLE guacamole_user_group (

      user_group_id  serial       NOT NULL,
      entity_id      integer      NOT NULL,
      disabled       boolean      NOT NULL DEFAULT FALSE,

      PRIMARY KEY (user_group_id)

    );

    --
    -- Table of entities. Each entity corresponds to either a user or user
    -- group, and ties that user or user group to its set of permissions.
    --

    CREATE TABLE guacamole_entity (

      entity_id   serial                  NOT NULL,
      name        varchar(128)            NOT NULL,
      type        varchar(32)             NOT NULL,

      PRIMARY KEY (entity_id),

      CONSTRAINT guacamole_entity_name_scope
          UNIQUE (type, name)

    );

    ALTER TABLE guacamole_user
        ADD CONSTRAINT guacamole_user_entity
        FOREIGN KEY (entity_id)
        REFERENCES guacamole_entity (entity_id) MATCH SIMPLE
        ON UPDATE NO ACTION ON DELETE CASCADE;

    ALTER TABLE guacamole_user_group
        ADD CONSTRAINT guacamole_user_group_entity
        FOREIGN KEY (entity_id)
        REFERENCES guacamole_entity (entity_id) MATCH SIMPLE
        ON UPDATE NO ACTION ON DELETE CASCADE;

    --
    -- Table of user group members.
    --

    CREATE TABLE guacamole_user_group_member (

      user_group_id    integer NOT NULL,
      member_entity_id integer NOT NULL,

      PRIMARY KEY (user_group_id, member_entity_id),

      CONSTRAINT guacamole_user_group_member_ibfk_1
          FOREIGN KEY (user_group_id)
          REFERENCES guacamole_user_group (user_group_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE,

      CONSTRAINT guacamole_user_group_member_ibfk_2
          FOREIGN KEY (member_entity_id)
          REFERENCES guacamole_entity (entity_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE

    );

    --
    -- Table of sharing profiles. Each sharing profile has a name and is
    -- associated with a connection that it can be used to share.
    --

    CREATE TABLE guacamole_sharing_profile (

      sharing_profile_id    serial       NOT NULL,
      sharing_profile_name  varchar(128) NOT NULL,
      primary_connection_id integer      NOT NULL,

      PRIMARY KEY (sharing_profile_id),

      CONSTRAINT sharing_profile_name_primary UNIQUE (sharing_profile_name, primary_connection_id),

      CONSTRAINT guacamole_sharing_profile_ibfk_1
          FOREIGN KEY (primary_connection_id)
          REFERENCES guacamole_connection (connection_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE

    );

    --
    -- Table of connection parameters. Each parameter has a name/value pair
    -- and is associated with a single connection.
    --

    CREATE TABLE guacamole_connection_parameter (

      connection_id   integer      NOT NULL,
      parameter_name  varchar(128) NOT NULL,
      parameter_value varchar(4096) NOT NULL,

      PRIMARY KEY (connection_id, parameter_name),

      CONSTRAINT guacamole_connection_parameter_ibfk_1
          FOREIGN KEY (connection_id)
          REFERENCES guacamole_connection (connection_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE

    );

    --
    -- Table of sharing profile parameters. Each parameter has a name/value
    -- pair and is associated with a single sharing profile.
    --

    CREATE TABLE guacamole_sharing_profile_parameter (

      sharing_profile_id integer       NOT NULL,
      parameter_name     varchar(128)  NOT NULL,
      parameter_value    varchar(4096) NOT NULL,

      PRIMARY KEY (sharing_profile_id, parameter_name),

      CONSTRAINT guacamole_sharing_profile_parameter_ibfk_1
          FOREIGN KEY (sharing_profile_id)
          REFERENCES guacamole_sharing_profile (sharing_profile_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE

    );

    --
    -- Table of arbitrary user attributes. Each attribute has a name/value
    -- pair and is associated with a single user. Arbitrary attributes are
    -- defined by extensions, and are not checked for validity by the storage
    -- layer.
    --

    CREATE TABLE guacamole_user_attribute (

      user_id         integer       NOT NULL,
      attribute_name  varchar(128)  NOT NULL,
      attribute_value varchar(4096) NOT NULL,

      PRIMARY KEY (user_id, attribute_name),

      CONSTRAINT guacamole_user_attribute_ibfk_1
          FOREIGN KEY (user_id)
          REFERENCES guacamole_user (user_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE

    );

    --
    -- Table of arbitrary user group attributes. Each attribute has a
    -- name/value pair and is associated with a single user group. Arbitrary
    -- attributes are defined by extensions, and are not checked for validity
    -- by the storage layer.
    --

    CREATE TABLE guacamole_user_group_attribute (

      user_group_id   integer       NOT NULL,
      attribute_name  varchar(128)  NOT NULL,
      attribute_value varchar(4096) NOT NULL,

      PRIMARY KEY (user_group_id, attribute_name),

      CONSTRAINT guacamole_user_group_attribute_ibfk_1
          FOREIGN KEY (user_group_id)
          REFERENCES guacamole_user_group (user_group_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE

    );

    --
    -- Table of arbitrary connection attributes. Each attribute has a
    -- name/value pair and is associated with a single connection. Arbitrary
    -- attributes are defined by extensions, and are not checked for validity
    -- by the storage layer.
    --

    CREATE TABLE guacamole_connection_attribute (

      connection_id   integer       NOT NULL,
      attribute_name  varchar(128)  NOT NULL,
      attribute_value varchar(4096) NOT NULL,

      PRIMARY KEY (connection_id, attribute_name),

      CONSTRAINT guacamole_connection_attribute_ibfk_1
          FOREIGN KEY (connection_id)
          REFERENCES guacamole_connection (connection_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE

    );

    --
    -- Table of arbitrary connection group attributes. Each attribute has a
    -- name/value pair and is associated with a single connection group.
    -- Arbitrary attributes are defined by extensions, and are not checked for
    -- validity by the storage layer.
    --

    CREATE TABLE guacamole_connection_group_attribute (

      connection_group_id integer       NOT NULL,
      attribute_name      varchar(128)  NOT NULL,
      attribute_value     varchar(4096) NOT NULL,

      PRIMARY KEY (connection_group_id, attribute_name),

      CONSTRAINT guacamole_connection_group_attribute_ibfk_1
          FOREIGN KEY (connection_group_id)
          REFERENCES guacamole_connection_group (connection_group_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE

    );

    --
    -- Table of arbitrary sharing profile attributes. Each attribute has a
    -- name/value pair and is associated with a single sharing profile.
    -- Arbitrary attributes are defined by extensions, and are not checked for
    -- validity by the storage layer.
    --

    CREATE TABLE guacamole_sharing_profile_attribute (

      sharing_profile_id integer       NOT NULL,
      attribute_name     varchar(128)  NOT NULL,
      attribute_value    varchar(4096) NOT NULL,

      PRIMARY KEY (sharing_profile_id, attribute_name),

      CONSTRAINT guacamole_sharing_profile_attribute_ibfk_1
          FOREIGN KEY (sharing_profile_id)
          REFERENCES guacamole_sharing_profile (sharing_profile_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE

    );

    --
    -- Table of connection permissions. Each connection permission grants a user
    -- or user group specific access to a connection.
    --

    CREATE TABLE guacamole_connection_permission (

      entity_id     integer NOT NULL,
      connection_id integer NOT NULL,
      permission    varchar(32) NOT NULL,

      PRIMARY KEY (entity_id, connection_id, permission),

      CONSTRAINT guacamole_connection_permission_ibfk_1
          FOREIGN KEY (connection_id)
          REFERENCES guacamole_connection (connection_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE,

      CONSTRAINT guacamole_connection_permission_ibfk_2
          FOREIGN KEY (entity_id)
          REFERENCES guacamole_entity (entity_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE

    );

    --
    -- Table of connection group permissions. Each connection group permission
    -- grants a user or user group specific access to a connection group.
    --

    CREATE TABLE guacamole_connection_group_permission (

      entity_id           integer NOT NULL,
      connection_group_id integer NOT NULL,
      permission          varchar(32) NOT NULL,

      PRIMARY KEY (entity_id, connection_group_id, permission),

      CONSTRAINT guacamole_connection_group_permission_ibfk_1
          FOREIGN KEY (connection_group_id)
          REFERENCES guacamole_connection_group (connection_group_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE,

      CONSTRAINT guacamole_connection_group_permission_ibfk_2
          FOREIGN KEY (entity_id)
          REFERENCES guacamole_entity (entity_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE

    );

    --
    -- Table of sharing profile permissions. Each sharing profile permission
    -- grants a user or user group specific access to a sharing profile.
    --

    CREATE TABLE guacamole_sharing_profile_permission (

      entity_id          integer NOT NULL,
      sharing_profile_id integer NOT NULL,
      permission         varchar(32) NOT NULL,

      PRIMARY KEY (entity_id, sharing_profile_id, permission),

      CONSTRAINT guacamole_sharing_profile_permission_ibfk_1
          FOREIGN KEY (sharing_profile_id)
          REFERENCES guacamole_sharing_profile (sharing_profile_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE,

      CONSTRAINT guacamole_sharing_profile_permission_ibfk_2
          FOREIGN KEY (entity_id)
          REFERENCES guacamole_entity (entity_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE

    );

    --
    -- Table of system permissions. Each system permission grants a user or
    -- user group a system-level privilege of some kind.
    --

    CREATE TABLE guacamole_system_permission (

      entity_id  integer     NOT NULL,
      permission varchar(32) NOT NULL,

      PRIMARY KEY (entity_id, permission),

      CONSTRAINT guacamole_system_permission_ibfk_1
          FOREIGN KEY (entity_id)
          REFERENCES guacamole_entity (entity_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE

    );

    --
    -- Table of user permissions. Each user permission grants a user or user
    -- group access of some kind to another user.
    --

    CREATE TABLE guacamole_user_permission (

      entity_id        integer NOT NULL,
      affected_user_id integer NOT NULL,
      permission       varchar(32) NOT NULL,

      PRIMARY KEY (entity_id, affected_user_id, permission),

      CONSTRAINT guacamole_user_permission_ibfk_1
          FOREIGN KEY (affected_user_id)
          REFERENCES guacamole_user (user_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE,

      CONSTRAINT guacamole_user_permission_ibfk_2
          FOREIGN KEY (entity_id)
          REFERENCES guacamole_entity (entity_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE

    );

    --
    -- Table of user group permissions. Each user group permission grants a
    -- user or user group access of some kind to a user group.
    --

    CREATE TABLE guacamole_user_group_permission (

      entity_id            integer NOT NULL,
      affected_user_group_id integer NOT NULL,
      permission           varchar(32) NOT NULL,

      PRIMARY KEY (entity_id, affected_user_group_id, permission),

      CONSTRAINT guacamole_user_group_permission_ibfk_1
          FOREIGN KEY (affected_user_group_id)
          REFERENCES guacamole_user_group (user_group_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE,

      CONSTRAINT guacamole_user_group_permission_ibfk_2
          FOREIGN KEY (entity_id)
          REFERENCES guacamole_entity (entity_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE

    );

    --
    -- Table of connection history records. Each record defines the connection
    -- made, its duration, and the user who made it.
    --

    CREATE TABLE guacamole_connection_history (

      history_id           serial        NOT NULL,
      user_id              integer       DEFAULT NULL,
      username             varchar(128)  NOT NULL,
      remote_host          varchar(256)  DEFAULT NULL,
      connection_id        integer       DEFAULT NULL,
      connection_name      varchar(128)  NOT NULL,
      sharing_profile_id   integer       DEFAULT NULL,
      sharing_profile_name varchar(128)  DEFAULT NULL,
      start_date           timestamptz   NOT NULL,
      end_date             timestamptz   DEFAULT NULL,

      PRIMARY KEY (history_id),

      CONSTRAINT guacamole_connection_history_ibfk_1
          FOREIGN KEY (user_id)
          REFERENCES guacamole_user (user_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE SET NULL,

      CONSTRAINT guacamole_connection_history_ibfk_2
          FOREIGN KEY (connection_id)
          REFERENCES guacamole_connection (connection_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE SET NULL,

      CONSTRAINT guacamole_connection_history_ibfk_3
          FOREIGN KEY (sharing_profile_id)
          REFERENCES guacamole_sharing_profile (sharing_profile_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE SET NULL

    );

    --
    -- Table of user login history records. Each record defines the login,
    -- its duration, and the IP address from which the user connected.
    --

    CREATE TABLE guacamole_user_history (

      history_id  serial       NOT NULL,
      user_id     integer      DEFAULT NULL,
      username    varchar(128) NOT NULL,
      remote_host varchar(256) DEFAULT NULL,
      start_date  timestamptz  NOT NULL,
      end_date    timestamptz  DEFAULT NULL,

      PRIMARY KEY (history_id),

      CONSTRAINT guacamole_user_history_ibfk_1
          FOREIGN KEY (user_id)
          REFERENCES guacamole_user (user_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE SET NULL

    );

    --
    -- User password history
    --

    CREATE TABLE guacamole_user_password_history (

      password_history_id serial  NOT NULL,
      user_id             integer NOT NULL,

      -- Hashed password
      password_hash bytea NOT NULL,
      password_salt bytea,
      password_date timestamptz NOT NULL,

      PRIMARY KEY (password_history_id),

      CONSTRAINT guacamole_user_password_history_ibfk_1
          FOREIGN KEY (user_id)
          REFERENCES guacamole_user (user_id) MATCH SIMPLE
          ON UPDATE NO ACTION ON DELETE CASCADE

    );

    -- Create default admin user "guacadmin" with password "guacadmin"
    INSERT INTO guacamole_entity (name, type) VALUES ('guacadmin', 'USER');
    INSERT INTO guacamole_user (entity_id, password_hash, password_salt, password_date)
    SELECT
        entity_id,
        decode('CA458A7D494E3BE824F5E1E175A1556C0F8EEF2C2D7DF3633BEC4A29C4411960', 'hex'),
        decode('FE24ADC5E11E2B25288D1704ABE67A79E342ECC26064CE69C5B3177795A82264', 'hex'),
        CURRENT_TIMESTAMP
    FROM guacamole_entity WHERE name = 'guacadmin' AND type = 'USER';

    -- Grant guacadmin full system permissions
    INSERT INTO guacamole_system_permission (entity_id, permission)
    SELECT entity_id, permission
    FROM (
        VALUES
            ('guacadmin', 'CREATE_CONNECTION'),
            ('guacadmin', 'CREATE_CONNECTION_GROUP'),
            ('guacadmin', 'CREATE_SHARING_PROFILE'),
            ('guacadmin', 'CREATE_USER'),
            ('guacadmin', 'CREATE_USER_GROUP'),
            ('guacadmin', 'ADMINISTER')
    ) permissions (username, permission)
    JOIN guacamole_entity ON guacamole_entity.name = permissions.username AND guacamole_entity.type = 'USER';

    -- Grant guacadmin READ/UPDATE/DELETE/ADMINISTER on guacadmin itself
    INSERT INTO guacamole_user_permission (entity_id, affected_user_id, permission)
    SELECT guacamole_entity.entity_id, guacamole_user.user_id, permission
    FROM (
        VALUES
            ('guacadmin', 'READ'),
            ('guacadmin', 'UPDATE'),
            ('guacadmin', 'DELETE'),
            ('guacadmin', 'ADMINISTER')
    ) permissions (username, permission)
    JOIN guacamole_entity      ON guacamole_entity.name = permissions.username AND guacamole_entity.type = 'USER'
    JOIN guacamole_user        ON guacamole_user.entity_id = guacamole_entity.entity_id;
